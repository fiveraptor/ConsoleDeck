<Page x:Class="ConsoleDeck.Views.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:vm="clr-namespace:ConsoleDeck.ViewModels"
      Title="Dashboard">

  <Page.Resources>
    <Style x:Key="ButtonCardStyle" TargetType="Button">
      <Setter Property="Background" Value="#1E1E1E"/>
      <Setter Property="BorderBrush" Value="#333333"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="8">
              <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#2A2A2A"/>
                <Setter Property="BorderBrush" Value="#555555"/>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#333333"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Page.Resources>

  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="Buttons" FontSize="20" FontWeight="SemiBold"
               Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}" Margin="0,0,0,20"/>

    <ItemsControl Grid.Row="1" ItemsSource="{Binding Buttons}">
      <ItemsControl.ItemsPanel>
        <ItemsPanelTemplate>
          <UniformGrid Columns="3" Rows="3"/>
        </ItemsPanelTemplate>
      </ItemsControl.ItemsPanel>
      <ItemsControl.ItemTemplate>
        <DataTemplate DataType="{x:Type vm:ButtonViewModel}">
          <Button Style="{StaticResource ButtonCardStyle}"
                  Margin="6"
                  MinHeight="110"
                  Click="ButtonCard_Click"
                  Tag="{Binding Number}">
            <Grid Margin="14,12">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
              </Grid.RowDefinitions>

              <TextBlock Grid.Row="0" Text="{Binding Number}"
                         FontSize="11" Foreground="#666666" FontWeight="Medium"/>

              <Border Grid.Row="1" Background="{Binding TypeBrush}" CornerRadius="4"
                      Padding="6,3" HorizontalAlignment="Left" Margin="0,8,0,8">
                <TextBlock Text="{Binding TypeLabel}" FontSize="11" FontWeight="Bold"
                           Foreground="White"/>
              </Border>

              <TextBlock Grid.Row="2" Text="{Binding Preview}"
                         FontSize="12" Foreground="#AAAAAA"
                         TextTrimming="CharacterEllipsis"
                         MaxWidth="160"/>
            </Grid>
          </Button>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>

    <TextBlock Grid.Row="2" Margin="0,16,0,0"
               Text="Media-Button (physisch) → immer Play/Pause, nicht konfigurierbar"
               FontSize="11" Foreground="#555555" HorizontalAlignment="Center"/>
  </Grid>
</Page>
